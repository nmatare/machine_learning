# Lecture 1 

# take note to the standardize the features so that they are comparable; obviously don't do this for forests

# CART algorithim
# impurity measure is a measure of homogenous each split is; that is you want a split that discriminates
# well ==> this is the measure of impurity 